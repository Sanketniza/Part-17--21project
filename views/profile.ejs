
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>sanket</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body class="w-[100%]  bg-zinc-900 "> 
    <div class=" w-[100%] container bg-zind-400 ml-5 ">

        <div class=" w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-5 py-3 mx-5 my-2 mx-1 border-2 border-zinc-700" href="/logout">Logout</a>
        </div>

        <div class="flex items-center">

            <div class="w-10 h-10 rounded-md ml-5 overflow-hidden">
                <img class="w-full h-full object-cover" src="/image/uploads/<%= user.profilePic %>" alt="">
            </div>

            <h1 class="text-3xl  text-white px-5 mt-2">     
              <span class="text-yellow-500">Hello </span> <%= user.name %>👋👋
            </h1>
        </div>

        <h4 class="text-xl  text-white px-5 mt-2">You can create new post</h4>
       
         <form action="/post" method="post">
            <textarea class="w-1/3 bg-zinc-800 block mx-5 my-5 text-white outline-none p-3 resize-none" placeholder="write something" name="content" id=""></textarea>
            <input class="w-[142px] px-5 py-3 mx-5 my-2 mx-1 border-2 border-zinc-700 bg-blue-500 border-2 outline-none rounded-md " type="submit" aria-valuenow="submit">
         </form>

         <div class=" w-100 post mt-10" >
              <h3 class="text-green-400 text-xl my-5 px-5 mt-0">Your post</h3>
              <div class="postcontainer flex flex-wrap my-5">

                <% user.post.reverse().forEach(function(post)  { %>

                    <div class="post w-1/3 p-4 mx-5  my-5 rounded-md border-2 border-zinc-700 bg-zinc-800">
                        <h4 class="text-blue-400 mb-2 "><%= user.username %></h4>
                        <p class="text-sm tracking-tight text-white" ><%= post.content %></p>
                          <small class="text-white inline-block text-sm mt-2"><%= post.likes.length %> Likes</small>
                            <div class="btn gap-5 flex ">
                                <a class="text-blue-500 " href="/like/<%= post._id %>">
                                    <%= post.likes.includes(user._id) ? 'Unlike' : 'Like' %>
                                </a>
                                <a class="text-yellow-500" href="/edit/<%= post._id %>">Edit</a>
                            </div>
                     </div>
                     
                     <% }) %>
                </div>
         </div>

    </div>
</body>
</html>